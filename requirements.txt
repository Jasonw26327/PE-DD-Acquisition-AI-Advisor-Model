# CPU stack. Enough to run the whole testbed on distilgpt2 on a laptop.
# Install torch first from the CPU index, then the rest:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Verified working on Python 3.10 with torch 2.13.0+cpu, transformers 5.14.1,
# peft 0.20.0, datasets 5.0.1.

transformers>=4.44
peft>=0.12
datasets>=2.20
accelerate>=0.33
numpy>=1.24
